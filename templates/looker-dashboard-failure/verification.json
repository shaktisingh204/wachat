{
  "schemaVersion": 1,
  "templateId": "looker-dashboard-failure",
  "cases": [
    {
      "id": "happy-path",
      "description": "Looker schedule failure posts to Slack #data.",
      "triggerPayload": {
        "dashboard_id": "12345",
        "dashboard_name": "Exec KPI",
        "failure_reason": "timeout"
      },
      "expect": { "finalStatus": "success" }
    }
  ]
}
