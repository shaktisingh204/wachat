{
  "schemaVersion": 1,
  "templateId": "redis-key-eviction-alert",
  "cases": [
    {
      "id": "happy-path",
      "description": "Redis maxmemory alert posts to Slack and opens a Linear ticket.",
      "triggerPayload": {
        "cluster": "cache-prod",
        "evictedKeys": 1200,
        "used_memory_human": "14.8G"
      },
      "expect": { "finalStatus": "success" }
    }
  ]
}
