{
  "schemaVersion": 1,
  "templateId": "rabbitmq-dlq-replay",
  "cases": [
    {
      "id": "happy-path",
      "description": "DLQ message is replayed to its original queue after a 5-minute wait.",
      "triggerPayload": {
        "headers": { "x-original-queue": "orders" },
        "messageId": "msg-001",
        "payload": { "orderId": 99 }
      },
      "expect": { "finalStatus": "success" }
    }
  ]
}
