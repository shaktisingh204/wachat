{
  "schemaVersion": 1,
  "templateId": "convertkit-tag-sync",
  "cases": [
    {
      "id": "tags-synced",
      "description": "New ConvertKit subscriber is upserted into Mailchimp with mirrored tags.",
      "triggerPayload": {
        "subscriber": {
          "email_address": "fan@example.com",
          "first_name": "Sam",
          "tags": [{ "name": "newsletter" }, { "name": "ai-track" }]
        }
      },
      "expect": { "finalStatus": "success" }
    }
  ]
}
