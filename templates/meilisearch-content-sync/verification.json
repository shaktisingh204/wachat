{
  "schemaVersion": 1,
  "templateId": "meilisearch-content-sync",
  "cases": [
    {
      "id": "happy-path",
      "description": "Mongo change webhook upserts (or deletes) into Meilisearch.",
      "triggerPayload": {
        "operationType": "update",
        "documentKey": { "_id": "doc-001" },
        "ns": { "coll": "articles" }
      },
      "expect": { "finalStatus": "success" }
    }
  ]
}
