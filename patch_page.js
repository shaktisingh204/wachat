const fs = require('fs');
const file = 'src/app/sabwa/status/page.tsx';
let content = fs.readFileSync(file, 'utf8');

// replace posted state with useEffect fetching
// add viewers modal
// add reply modal

