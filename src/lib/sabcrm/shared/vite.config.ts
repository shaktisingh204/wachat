// PORT-NOTE: This file was the Vite library build config for the twenty-shared
// package (Nx monorepo). It built ESM + CJS bundles from src/index.ts and
// each submodule index. SabNode uses Next.js (not a standalone library build),
// so this config is unused. Shared CRM utilities are consumed directly via
// src/lib/sabcrm/shared/src/... imports inside the Next.js app.
// This stub is kept so the 1:1 file mapping remains complete and typechecks.

export {};
