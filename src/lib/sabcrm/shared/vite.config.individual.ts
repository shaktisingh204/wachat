// PORT-NOTE: This file was the Vite individual-entry build config for the
// twenty-shared package (Nx monorepo). SabNode uses Next.js and does not
// ship twenty-shared as a separate npm package or standalone Vite build.
// The module-splitting / individual-entry pattern has no equivalent here —
// shared utilities are imported directly from their SabNode target paths
// (src/lib/sabcrm/shared/src/...). This stub is kept so the 1:1 file
// mapping remains complete.

export {};
