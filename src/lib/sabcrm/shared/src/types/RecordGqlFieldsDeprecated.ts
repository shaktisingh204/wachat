// PORT-NOTE: Loose record type retained from original — `any` is intentional here
// as the source itself used Record<string, any> for backward compatibility.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type RecordGqlFieldsDeprecated = Record<string, any>;
