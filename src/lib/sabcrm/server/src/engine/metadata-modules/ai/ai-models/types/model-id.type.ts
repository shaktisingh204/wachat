// PORT-NOTE: Ported from twenty-server ai-models/types/model-id.type.ts
// Composite workspace model identifier, usually `provider/modelName`
// (see buildCompositeModelId util).

export type ModelId = string;
