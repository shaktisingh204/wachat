// PORT-NOTE: Ported from twenty-server ai-models/constants/ai-telemetry.const.ts
// Plain constant — no NestJS or Mongo dependency.

export const AI_TELEMETRY_CONFIG = {
  isEnabled: true,
  recordInputs: true,
  recordOutputs: true,
};
