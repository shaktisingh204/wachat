// PORT-NOTE: Ported from twenty-server ai-agent/constants/agent-config.const.ts
// Plain constant — no NestJS or Mongo dependency.

export const AGENT_CONFIG = {
  MAX_STEPS: 300,
  REASONING_BUDGET_TOKENS: 12000,
};
