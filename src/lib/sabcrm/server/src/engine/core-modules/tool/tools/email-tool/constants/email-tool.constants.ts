// Re-exports the shared constant so downstream imports resolve against this
// target path (previously `twenty-shared/constants`).
export { MAX_EMAIL_RECIPIENTS } from "@/lib/sabcrm/shared/src/constants/MaxEmailRecipients";
