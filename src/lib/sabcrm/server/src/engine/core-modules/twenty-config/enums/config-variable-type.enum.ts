// PORT-NOTE: Ported from twenty-server. Pure TypeScript enum — no framework deps.
export enum ConfigVariableType {
  BOOLEAN = 'boolean',
  NUMBER = 'number',
  ARRAY = 'array',
  STRING = 'string',
  ENUM = 'enum',
  JSON = 'json',
}
