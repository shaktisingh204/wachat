// PORT-NOTE: dto — GraphQL @ObjectType DTO ported to plain TypeScript type.

export type DeleteTwoFactorAuthenticationMethodDTO = {
  /** Boolean that confirms query was dispatched */
  success: boolean;
};
