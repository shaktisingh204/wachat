import "server-only";

// PORT-NOTE: Standard user-facing error message used in query runner exceptions.
// Original used @lingui/core/macro `msg` template tag; replaced with plain string.
export const STANDARD_ERROR_MESSAGE =
  'An error occurred. Please try again or contact support if the problem persists.';
