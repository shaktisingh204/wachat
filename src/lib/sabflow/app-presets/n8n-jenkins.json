{
  "id": "n8n-jenkins",
  "name": "Jenkins",
  "description": "Consume Jenkins API",
  "category": "dev",
  "iconName": "LuPackage",
  "version": 4,
  "lastVerified": "2026-06-11",
  "status": "draft",
  "auth": {
    "type": "header",
    "credentialType": "http_header_auth"
  },
  "baseUrl": "https://{host}",
  "endpoints": [
    {
      "id": "job_copy",
      "label": "Copy Job",
      "method": "POST",
      "path": "/createItem",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        },
        {
          "id": "job",
          "label": "Job Name or ID",
          "type": "select",
          "required": true,
          "description": "Name of the job. Choose from the list, or specify an ID using an expression."
        },
        {
          "id": "newJob",
          "label": "New Job Name",
          "type": "text",
          "required": true,
          "description": "Name of the new Jenkins job"
        }
      ],
      "group": "Job",
      "description": "Copy a specific job"
    },
    {
      "id": "job_create",
      "label": "Create Job",
      "method": "POST",
      "path": "/createItem",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        },
        {
          "id": "newJob",
          "label": "New Job Name",
          "type": "text",
          "required": true,
          "description": "Name of the new Jenkins job"
        },
        {
          "id": "xml",
          "label": "XML",
          "type": "text",
          "required": true,
          "description": "XML of Jenkins config"
        }
      ],
      "group": "Job",
      "description": "Create a new job"
    },
    {
      "id": "job_trigger",
      "label": "Trigger Job",
      "method": "POST",
      "path": "/job/{job}/build",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        },
        {
          "id": "job",
          "label": "Job Name or ID",
          "type": "select",
          "required": true,
          "description": "Name of the job. Choose from the list, or specify an ID using an expression.",
          "in": "path"
        }
      ],
      "group": "Job",
      "description": "Trigger a specific job"
    },
    {
      "id": "job_trigger_params",
      "label": "Trigger with Parameters Job",
      "method": "POST",
      "path": "/job/{job}/buildWithParameters",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        },
        {
          "id": "job",
          "label": "Job Name or ID",
          "type": "select",
          "required": true,
          "description": "Name of the job. Choose from the list, or specify an ID using an expression.",
          "in": "path"
        },
        {
          "id": "param",
          "label": "Parameters",
          "type": "json",
          "required": true,
          "placeholder": "Add Parameter",
          "description": "Parameters for Jenkins job (JSON object; supported keys: params, params.name, params.value)"
        }
      ],
      "group": "Job",
      "description": "Trigger a specific job"
    },
    {
      "id": "instance_cancel_quiet_down",
      "label": "Cancel Quiet Down Instance",
      "method": "POST",
      "path": "/cancelQuietDown",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        }
      ],
      "group": "Instance",
      "description": "Cancel quiet down state"
    },
    {
      "id": "instance_quiet_down",
      "label": "Quiet Down Instance",
      "method": "POST",
      "path": "/quietDown",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        },
        {
          "id": "reason",
          "label": "Reason",
          "type": "text",
          "description": "Freeform reason for quiet down mode"
        }
      ],
      "group": "Instance",
      "description": "Put Jenkins in quiet mode, no builds can be started, Jenkins is ready for shutdown"
    },
    {
      "id": "instance_restart",
      "label": "Restart Instance",
      "method": "POST",
      "path": "/restart",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        }
      ],
      "group": "Instance",
      "description": "Restart Jenkins immediately on environments where it is possible"
    },
    {
      "id": "instance_safe_restart",
      "label": "Safely Restart Instance",
      "method": "POST",
      "path": "/safeRestart",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        }
      ],
      "group": "Instance",
      "description": "Restart Jenkins once no jobs are running on environments where it is possible"
    },
    {
      "id": "instance_safe_exit",
      "label": "Safely Shutdown Instance",
      "method": "POST",
      "path": "/safeExit",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        }
      ],
      "group": "Instance",
      "description": "Shutdown once no jobs are running"
    },
    {
      "id": "instance_exit",
      "label": "Shutdown Instance",
      "method": "POST",
      "path": "/exit",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        }
      ],
      "group": "Instance",
      "description": "Shutdown Jenkins immediately"
    },
    {
      "id": "build_get_all",
      "label": "Get Many Build",
      "method": "GET",
      "path": "/job/{job}/api/json?tree=builds[*]",
      "fields": [
        {
          "id": "host",
          "label": "Host",
          "type": "text",
          "required": true,
          "in": "path",
          "description": "Host of your Jenkins server, e.g. app.example.com (no https:// prefix)"
        },
        {
          "id": "job",
          "label": "Job Name or ID",
          "type": "select",
          "required": true,
          "description": "Name of the job. Choose from the list, or specify an ID using an expression.",
          "in": "path"
        },
        {
          "id": "returnAll",
          "label": "Return All",
          "type": "toggle",
          "description": "Whether to return all results or only up to a given limit",
          "defaultValue": false
        },
        {
          "id": "limit",
          "label": "Limit",
          "type": "number",
          "description": "Max number of results to return",
          "defaultValue": 50
        }
      ],
      "group": "Build",
      "description": "List Builds"
    }
  ]
}
