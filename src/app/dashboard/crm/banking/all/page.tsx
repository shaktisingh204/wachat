import { BankingListClient } from './_components/banking-list-client';

/**
 * Banking landing — Banking landing page (§1D thin bar).
 * KPI strip + tabs (Bank | Employee | Cash | Wallet | All).
 */
export default function BankingLandingPage() {
    return <BankingListClient />;
}
