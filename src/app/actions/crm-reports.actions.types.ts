/**
 * Types extracted from crm-reports.actions.ts
 * to avoid Turbopack 'use server' export restrictions.
 */

export interface TpReportProject { id: string; name: string }

export interface TpReportOwner  { id: string; name: string }
