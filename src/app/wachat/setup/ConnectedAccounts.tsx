// Kept as a stub for backwards compatibility. The connected-accounts
// surface now renders inline inside `page.client.tsx`.
import React from 'react';

export default function ConnectedAccounts() {
  return null;
}
