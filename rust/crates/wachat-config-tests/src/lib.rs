//! `wachat-config-tests` — Phase 5 slice 9.
//!
//! End-to-end integration tests for the wachat configuration HTTP surface.
//! All meaningful code lives under `tests/end_to_end.rs`; this library
//! target is intentionally empty so `cargo build -p wachat-config-tests`
//! is a near-no-op and only `cargo test` pulls in the full wachat-config
//! stack.
