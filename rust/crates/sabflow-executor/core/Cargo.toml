[package]
name = "sabflow-executor-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "SabFlow executor — core scaffold (Track B Phase 1). Workflow lifecycle and execution context types land in later phases."

[lib]
name = "sabflow_executor_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
thiserror = "2.0.18"

[lints]
workspace = true
