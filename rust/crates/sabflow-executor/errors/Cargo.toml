[package]
name = "sabflow-executor-errors"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "SabFlow executor — shared error types scaffold (Track B Phase 1). Concrete error variants land in later phases."

[lib]
name = "sabflow_executor_errors"
path = "src/lib.rs"

[dependencies]

[lints]
workspace = true
