# SabCall local Routr seed — Credentials.
# LOCAL DEV ONLY — change these and, in production, inject from SabVault
# (the engine's routr.json emits references, never plaintext).
- apiVersion: v2beta1
  kind: Credentials
  ref: credentials-asterisk
  metadata:
    name: Asterisk media peer
  spec:
    credentials:
      username: asterisk
      password: changeme

- apiVersion: v2beta1
  kind: Credentials
  ref: credentials-1001
  metadata:
    name: Test agent 1001
  spec:
    credentials:
      username: "1001"
      password: changeme
